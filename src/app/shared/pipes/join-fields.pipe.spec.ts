import { JoinFieldsPipe } from './join-fields.pipe';

describe('JoinFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
